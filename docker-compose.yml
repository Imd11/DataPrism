services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATA_WEAVER_DATA_DIR=/data
      - DATA_WEAVER_ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
    volumes:
      - ./.data-weaver:/data

